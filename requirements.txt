# Runtime dependencies for the reproducible CVE-to-ATT&CK pipeline.
# Supported interpreter target: Python 3.10-3.12.
#
# Exact versions used for the final paper run will be frozen later in
# requirements-lock.txt after the end-to-end smoke test succeeds.

# LLM extraction and reranking
openai>=2.0,<3.0
httpx>=0.28,<1.0

# Canonical X/y dataset construction and alignment audits
pandas>=2.2,<3.1

# Progress reporting for long extraction/reranking jobs
tqdm>=4.66,<5.0